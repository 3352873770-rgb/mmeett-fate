# Changelog

本项目采用语义化版本。用户可感知、架构、发布和项目治理变化均记录在此。

## Unreleased

- 修正股市项目一级界面定位：该页面用于宣传 AI 股市分析与判断辅助软件，不是投教首页；PRD 和功能地图改为围绕软件实景、图表上下文、研究工作流、分析能力、证据与反证、使用场景、可信分析和网页版转化，股市领域知识仅保留为内部产品背景。
- 移除人格偏好 Banner 的 LightRays / LiquidEther 动态特效，改用专门生成并压缩的低对比静态水墨山景；昼夜模式复用同一图片并通过主题滤镜保持暖米纸与深墨层级。
- 统一首页 Hero、下方内容区与人格偏好页的昼夜背景色家族，以底部渐变和低对比纹理降低相邻模块的颜色割裂；同时收紧人格页移动端 Banner 空白，并提高问卷说明、两端倾向和中立标签的可读性。
- 为桌面端首页增加一次性的 Banner 背景指针入场：沿“开始问卦”按钮中心高度在 900ms 内模拟鼠标左右往返，唤起当前 LightRays / LiquidEther 响应；按钮本身不移动，移动端、粗指针和减少动态模式不执行。
- 重构首页 Hero 中央字标为设计稿原始的自托管 Cormorant Garamond 600 单行 `MMEETT Fate`：`MMEETT` 正体大写、`Fate` 600 斜体，移除重复 “Fate” 说明行与所有中文品牌文字；导航官方 MMEETT 标识、昼夜主题、标语、CTA 与既有交互保持不变。
- 将三枚铜钱二级页的退出目标从首页 `#tools` 锚点改为站点根地址；返回后回到首页顶部，不再让同一网站的 Hash 地址被误解为第二个页面。
- 修复首页“长期问卦”和“推荐小工具”的重复、错位描边：移除伪元素边框与按钮外扩特效层，仅保留逐按钮单一描边及图标、文案、箭头微交互。
- 放大首页 Banner 的 MMEETT 官方字标：桌面端解除过低宽度上限并按视口扩大，手机端同步增加上限但保持容器内完整显示。
- 统一日间首页品牌对比：导航与 Banner 中的 MMEETT 官方字标均改为纯黑；导航文字和夜间旧金色保持不变。
- 调整首页 Banner 品牌层级：顶部导航保留完整 Logo，品牌说明改为“Fate”；中心只保留放大的 MMEETT 字标，成为首屏视觉焦点。
- 同步用户提供的 MMEETT 官方 Logo：首页导航、Hero 与二级页页眉统一改用图形标和自定义字标 SVG，移除不符合品牌稿的 Cormorant Garamond 字标依赖。
- 收紧移动端横向手势边界：问卦与工具按钮的描边画布不再超出按钮横向范围，首页对应区域及四个现役二级页统一只接受纵向页面手势，避免横向回弹，同时保留逐按钮高光。
- 为人格偏好、今日卦象、六十四卦知识和三枚铜钱二级页增加共享固定导航；长页面滚动过程中始终可见返回首页/工具与昼夜切换，支持中途退出当前流程。
- 修复手机端页面可被整页横向拖动的问题：约束 `html`、`body` 与 React/Legacy 挂载根节点的横向溢出和回弹，同时保留顶部导航自身的局部横向滚动。
- 将 GitHub 仓库和 Pages 子路径从 `dongfang-guanxing` 同步更名为 `mmeett-fate`，并修复品牌新公网地址的静态资源基础路径。
- 将产品品牌重塑为 MMEETT Fate：引入共享单行英文品牌字标、自托管 Cormorant Garamond 必要字重、更新首页 Hero 文案和 CTA，并同步现役二级页标题、SEO 与品牌文案；保留原有布局、昼夜主题和业务流程。
- 将首页六十四卦图谱改为无横向滚动的静态入口，并新增支持昼夜模式、搜索和 64 卦浏览的知识二级页。
- 将首页与六十四卦知识页的 LightRays / LiquidEther 限制在 Banner 内，并在 Banner 离开视口或页面进入后台后卸载 WebGL。
- 将“今日一卦”卡片按钮改为文案更简洁、点击属性更明确的金色圆角“今日卦象”CTA，并让日期自动对应用户本地年月日。
- 增加逐任务发布确认门禁：后续任务默认停在本地验证和草稿 PR，只有用户明确确认当次发布后才更新 GitHub Pages 公网版本。
- 增加可操作的“开始观星问卦”四步流程：选择所问、填写具体问题、选择或新建人物档案、确认起卦方式；档案下拉位于“基本信息”上方，切换后自动回填可编辑的出生信息。
- 问卦流程四个状态统一使用“·观星问卦 ·”页眉，不再显示步骤或阶段提示。
- 问卦流程收敛为单页完整表单，保存档案后直接展示卦象结果；长期问卦入口仅预选所问方向。
- 将人物档案抽为共享组件，并接入“云签解惑、事业灵签、流年运势、时辰运势、AI 解读报告”五个二级工具；输入页统一复用问卦排版，结果页按任务分层展示。
- 增加可重算的时间起卦、本地问卦记录和读取真实记录的结构化 AI 报告原型；未接入外部模型时明确显示原型状态。
- 将首页“今日卦象”接入完整二级结果页：同一份本地日期数据同步首页与详情页，并连续展示卦象结构、经典原文、现代观察、理性边界和对应六十四卦知识入口。
- 将人物档案共享组件统一置于五个“灵签信”二级工具的首个表单位置，生成结果时保存并关联当前档案，同时保持各工具原有起卦与报告口径不变。
- 建立 `agent/prototype-current` 作为未发布功能的唯一集成分支；各功能在独立分支完成后合回该分支，最终再按单次授权发布到 `main`。
- 统一首页与二级流程的昼夜主题继承：观星问卦和五个灵签工具现在复用同一组主题变量，首页选择昼或夜后进入功能仍保持对应的米纸或深墨背景。
- 将“推荐小工具”中的“三枚铜钱”接入单页完整任务流：同页完成定问、六次掷币和见卦，结果直接在当前页展开并保留全部输入与六爻记录。
- 将首页“人格图谱”接入单页人格偏好探索：12 道题采用“两端文字 + 中间五点”的语义化量表，一次提交后生成四轴偏好印谱、可选边界核对题与连续人格介绍。
- 将已验收的首页、问卦、灵签信、今日卦象、六十四卦、人物档案、三枚铜钱与人格偏好探索统一纳入 PR #30 的 GitHub Pages 发布范围。
- 修复首页人格卡片懒加载前无预留高度导致的透明、不可点击和布局位移，并改用稳定根节点事件委托。
- 清理首页人格入口的重复初始化与无效路由监听，并将 LiquidEther 从弃用的 Three.js Clock 迁移到 Timer。
- 重绘首页人格测试入口图，将旧八维卦象雷达更新为与二级页一致的四轴偏好印谱，并同步非诊断边界文案。

## 1.0.0 - 2026-07-21

- 建立根目录唯一源码，将未跟踪的重复项目副本移出工作区并保留可恢复备份。
- 增加项目状态、PRD、路线图、设计系统、架构、组件地图、质量和发布文档。
- 增加自动测试、统一 `npm run check` 与 GitHub 质量工作流。
- 将质量检查与 Pages 发布工作流升级到 Node.js 24 和当前 GitHub Actions 主版本。
- 完成东方观星一级首页的昼夜双主题体验。
- 完成移动端适配、首页书名漂移和逐按钮交互动效。
- 发布 GitHub Pages 公网版本。
- 优化移动端渲染与 Legacy 静态资源加载。

# 变更记录

项目当前版本仍为 `0.0.0`，在正式建立版本规则前，先通过本文件明确记录变更。

## 未发布

- 建立长期项目管理基线：`AGENTS.md`、`PROJECT_STATE.md`、产品文档、工程文档、决策记录，以及 CCPM PRD/Epic。
- 新增 `docs/fate-function-map.md`，在重新设计前梳理当前复刻项目的功能。
- 新增标准本地质量检查命令 `npm run check`。
- 明确记录当前工作目录属于使用演示数据的前端原型，尚无经过验证的生产部署。
- 为 `fate-rebuild` Epic 创建 `001.md` 至 `008.md` 共 8 个 CCPM 任务文件。
- 处理本地 macOS 原生依赖隔离和 TypeScript 6 弃用配置后，已验证 `npm run check` 可在本地通过。
- 将新建的项目管理与规划文档中文化，方便产品与设计审阅；代码标识和结构化字段保持英文。
- 新增 `docs/homepage-function-map.md`，将首页方向固定为全局导航、今日命理场、意图推荐区、回访与每日使用、结果可信度展示、更多探索和页脚。
- 同步更新 PRD、路线图、设计系统、项目状态和 CCPM 任务说明，使首页重构以用户确认的新首页功能结构为准。
- 根据浏览器批注调整首页：首屏替换为用户提供的深谷光路图，热门推演收敛为 4 张功能卡和右侧介绍，问题区改为 5 条大按钮入口，移除回访型每日模块，并将每日一卦详情改为点击按钮后在同一背景层内展开。
- 将 WorkBuddy 静态首页的暗夜鎏金视觉与内容顺序复刻到 React 首页，同时保持现有 Banner 不变；新增古籍滚动横滑翻卡、按参考图左右交替的四个竖排工具入口，以及结果可信度模块，并完成桌面与移动端浏览器验收。
- 热门工具区改用接近 Banner 字色的暖白色调与 GSAP 分层文字入场，移除功能层外框、阴影和滚动叠压；问题入口新增覆盖整行的左进右退悬停横线，并补充无障碍减弱动效处理。
- 扩大四个热门工具层之间的垂直距离，并为卡片画面与竖排文字加入不同滚动速率的 GSAP ScrollTrigger 视差效果。
- 将 Banner 文案固定在导航下方的视觉中心；为每日一卦和古籍区增加区块间留白，并扩大每日文案层级与古籍标题到卡片之间的距离。
- 修复 Banner 到每日一卦之间的硬色带，将留白改为区块内部连续渐变；古籍卡片保持原有最小高度，通过释放容器纵向裁切解决底部遮挡。
- 古籍图片延伸为整张卡片背景，下方信息区改为半透明渐隐遮罩，在保持卡片尺寸和文字清晰度的同时展示更多完整画面。
- 古籍区改为 GSAP 驱动的整层上覆滚动转场，并使用稍亮的蓝灰黑背景、顶部圆角与细分界线，与上方黑色内容形成层次对比。
- 热门工具区同步采用古籍层的蓝灰黑色阶，使四个视差工具卡与相邻内容保持统一的背景层次。
- 每日一卦文字动画改为屏幕底边接触 `DAILY HEXAGRAM` 标签时才开始播放，避免在大段留白期间提前完成。
- 加强古籍滚动叠层的视觉对比：扩大上覆距离，提高蓝灰背景亮度，并强化顶部圆角、描边和投影。
- Banner 的 `Start` 按钮新增圆形扩散填充、悬停放大、暖白光晕和按下回弹效果。
- 删除 Banner 标题上方的“东方命理，云海问卦”标签。
- 增加 GitHub Pages 子路径构建配置、Router basename 和统一静态资源 helper，为 `/mmeett-fate/` 部署做准备。
- 为八字、紫微、六爻和塔罗四张热门工具卡加入统一的深靛黑无文字主题图片，并按卡片双倍显示密度压缩为 780 × 1040 JPEG 资源。
- 为首页六本古籍分别设计无文字主题图，将翻卡正面重排为图片与书目信息上下分区，并以 700 × 1080 JPEG 资源适配桌面与移动端阅读卡片。
